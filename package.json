{
  "name": "nav-self-growth-together",
  "private": true,
  "description": "Local dev server for the nav-self-growth-together meetup page",
  "scripts": {
    "sync": "node sync-content.js",
    "dev": "npm run sync && live-server --port=5500 --open=/index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.0"
  }
}
